{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "start:dev": "NODE_ENV=development node --env-file=.env.development server.js",
    "start:prod": "NODE_ENV=production node --env-file=.env.production server.js",
    "build": "cd ../frontend && npm run build",
    "build:full": "npm run build && npm run start:prod",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:email": "NODE_ENV=development node test-email.js",
    "test:email:prod": "NODE_ENV=production node test-email.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.3",
    "redis": "^5.1.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "prisma": "^6.8.2"
  }
}
